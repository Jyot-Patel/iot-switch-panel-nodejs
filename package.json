{
  "name": "iotserver",
  "version": "1.0.0",
  "description": "\"ÏoT project with ESP32 polling to node server . server acting as middleware between client (web browser from mobile) and device(ESP with sensors)\"",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "start"
  },
  "dependencies": {
    "express": "^5.2.1",
    "http": "^0.0.1-security"
  }
}
